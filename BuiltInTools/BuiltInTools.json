{
    "name": "Built-in Tools",
    "executable": "BuiltInTools.exe",
    "tools": [
        {
            "name": "run_shell_command",
            "description": "Execute a shell command on the host system and return its output.",
            "parameters": [
                { "name": "command", "type": "string", "description": "Full command line to execute. Keep it short and avoid interactive programs.", "required": true }
            ]
        },
    ]
}
