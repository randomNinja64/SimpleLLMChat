<Window x:Class="SimpleLLMChatGUI.ColorsForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Colors"
        Height="300"
        Width="360"
        MinHeight="300"
        MinWidth="360"
        MaxWidth="360"
        MaxHeight="300"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Loaded="Window_Loaded"
        Background="{DynamicResource WindowBackgroundColorBrush}">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Main Content -->
        <ScrollViewer Grid.Row="0"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="8">

                <!-- ROW TEMPLATE -->
                <!-- Button Text -->
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="70"/>
                        <ColumnDefinition Width="70"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Label Content="Button Text"
                           VerticalAlignment="Center"/>

                    <Button Grid.Column="1"
                            Content="Choose"
                            Height="23"
                            Margin="0,0,8,0"
                            Tag="buttontextcolor"
                            Click="ChooseColorButton_Click"/>

                    <Button Grid.Column="2"
                            Content="Clear"
                            Height="23"
                            Margin="0,0,8,0"
                            Tag="buttontextcolor"
                            Click="ClearColorButton_Click"/>

                    <Border x:Name="ButtonTextColorPreview"
                            Grid.Column="3"
                            Width="50"
                            Height="23"
                            BorderBrush="Gray"
                            BorderThickness="1"
                            VerticalAlignment="Center"/>
                </Grid>

                <!-- Chat Background -->
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="70"/>
                        <ColumnDefinition Width="70"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Label Content="Chat BG"
                           VerticalAlignment="Center"/>

                    <Button Grid.Column="1"
                            Content="Choose"
                            Height="23"
                            Margin="0,0,8,0"
                            Tag="chatbackgroundcolor"
                            Click="ChooseColorButton_Click"/>

                    <Button Grid.Column="2"
                            Content="Clear"
                            Height="23"
                            Margin="0,0,8,0"
                            Tag="chatbackgroundcolor"
                            Click="ClearColorButton_Click"/>

                    <Border x:Name="ChatBackgroundColorPreview"
                            Grid.Column="3"
                            Width="50"
                            Height="23"
                            BorderBrush="Gray"
                            BorderThickness="1"
                            VerticalAlignment="Center"/>
                </Grid>

                <!-- Chat Text -->
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="70"/>
                        <ColumnDefinition Width="70"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Label Content="Chat Text"
                           VerticalAlignment="Center"/>

                    <Button Grid.Column="1"
                            Content="Choose"
                            Height="23"
                            Margin="0,0,8,0"
                            Tag="chattextcolor"
                            Click="ChooseColorButton_Click"/>

                    <Button Grid.Column="2"
                            Content="Clear"
                            Height="23"
                            Margin="0,0,8,0"
                            Tag="chattextcolor"
                            Click="ClearColorButton_Click"/>

                    <Border x:Name="ChatTextColorPreview"
                            Grid.Column="3"
                            Width="50"
                            Height="23"
                            BorderBrush="Gray"
                            BorderThickness="1"
                            VerticalAlignment="Center"/>
                </Grid>

                <!-- Code Block BG -->
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="70"/>
                        <ColumnDefinition Width="70"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Label Content="Code Block BG"
                           VerticalAlignment="Center"/>

                    <Button Grid.Column="1"
                            Content="Choose"
                            Height="23"
                            Margin="0,0,8,0"
                            Tag="codeblockbackgroundcolor"
                            Click="ChooseColorButton_Click"/>

                    <Button Grid.Column="2"
                            Content="Clear"
                            Height="23"
                            Margin="0,0,8,0"
                            Tag="codeblockbackgroundcolor"
                            Click="ClearColorButton_Click"/>

                    <Border x:Name="CodeBlockBackgroundColorPreview"
                            Grid.Column="3"
                            Width="50"
                            Height="23"
                            BorderBrush="Gray"
                            BorderThickness="1"
                            VerticalAlignment="Center"/>
                </Grid>

                <!-- Label Text -->
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="70"/>
                        <ColumnDefinition Width="70"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Label Content="Label Text"
                           VerticalAlignment="Center"/>

                    <Button Grid.Column="1"
                            Content="Choose"
                            Height="23"
                            Margin="0,0,8,0"
                            Tag="labeltextcolor"
                            Click="ChooseColorButton_Click"/>

                    <Button Grid.Column="2"
                            Content="Clear"
                            Height="23"
                            Margin="0,0,8,0"
                            Tag="labeltextcolor"
                            Click="ClearColorButton_Click"/>

                    <Border x:Name="LabelTextColorPreview"
                            Grid.Column="3"
                            Width="50"
                            Height="23"
                            BorderBrush="Gray"
                            BorderThickness="1"
                            VerticalAlignment="Center"/>
                </Grid>

                <!-- Window BG -->
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="70"/>
                        <ColumnDefinition Width="70"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Label Content="Window BG"
                           VerticalAlignment="Center"/>

                    <Button Grid.Column="1"
                            Content="Choose"
                            Height="23"
                            Margin="0,0,8,0"
                            Tag="windowbackgroundcolor"
                            Click="ChooseColorButton_Click"/>

                    <Button Grid.Column="2"
                            Content="Clear"
                            Height="23"
                            Margin="0,0,8,0"
                            Tag="windowbackgroundcolor"
                            Click="ClearColorButton_Click"/>

                    <Border x:Name="WindowBackgroundColorPreview"
                            Grid.Column="3"
                            Width="50"
                            Height="23"
                            BorderBrush="Gray"
                            BorderThickness="1"
                            VerticalAlignment="Center"/>
                </Grid>

            </StackPanel>
        </ScrollViewer>

        <!-- Bottom Buttons -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="SaveButton"
                    Content="OK"
                    Width="75"
                    Height="23"
                    Margin="0,0,8,0"
                    Click="SaveButton_Click"
                    IsDefault="True"/>

            <Button x:Name="CancelButton"
                    Content="Cancel"
                    Width="75"
                    Height="23"
                    Click="CancelButton_Click"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
